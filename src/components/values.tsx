/**
 * MOVED to site/src/components/values.tsx
 *
 * The marketing site is now a separate, fully static project under site/ so it
 * can be shared without the MVP's DuckDB warehouse. This file is a stub kept
 * only because the dev server held it open at the time of the split.
 *
 * Safe to delete.
 */
export {};
