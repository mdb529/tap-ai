/**
 * DEPRECATED — superseded by ./walkthrough.tsx
 *
 * The original simulator showed a single tap in isolation. That undersold the
 * product: a tap on its own reads as a notification, and notifications are
 * cheap. The walkthrough puts the tap where it belongs — in the middle of a
 * chain that starts with a detected change and ends with a reviewed deploy —
 * and adds vertical-specific scenarios.
 *
 * Safe to delete. Kept only because the file was locked by a running process
 * when the replacement landed.
 */
export {};
